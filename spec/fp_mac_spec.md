# 浮点乘累加模块设计规格

## 1. 设计概述
- **模块名称**: fp_mac
- **功能描述**: 浮点乘累加
- **设计者**: Abu
- **版本**: 1.0
- **日期**: 2026-04-06

## 2. 接口信号
| 信号名 | 方向 | 宽度 | 描述 | 时钟域 |
|--------|------|------|------|--------|
| io_a | input | 32 | 输入操作数A，IEEE 754单精度浮点数 | 组合逻辑 |
| io_b | input | 32 | 输入操作数B，IEEE 754单精度浮点数 | 组合逻辑 |
| io_c | input | 32 | 输入操作数C，IEEE 754单精度浮点数 | 组合逻辑 |
| io_rnd | input | 3 | 舍入模式控制，具体编码见第3节 | 组合逻辑 |
| io_z | output | 32 | 计算结果，IEEE 754单精度浮点数 | 组合逻辑 |
| io_status | output | 8 | 状态标志，具体编码见第3节 | 组合逻辑 |

**参数**: 无

## 3. 功能描述

### 3.1 概述
本模块实现单精度浮点乘累加运算：`Z = A * B + C`，支持IEEE 754标准，包括正规数、次正规数、无穷大、NaN（Not a Number）的处理，并提供多种舍入模式。

### 3.2 算法流程
1. **输入分解**：将输入A、B、C分解为符号位、指数位、尾数位，并添加隐藏位（当指数非零时为1，否则为0）。
2. **乘法计算**：
   - 计算乘积符号：`product_sign = a_sign ^ b_sign`
   - 计算乘积指数：`product_exp = a_exp + b_exp - 127`（扣除偏置）
   - 计算乘积尾数：`product_man = {a_hidden, a_man} * {b_hidden, b_man}`（24位×24位乘法，结果48位，然后左移扩展为72位）
3. **尾数对齐**：
   - 计算指数差：`exp_diff = |product_exp - c_exp|`
   - 根据指数差对乘积尾数或C尾数进行右移对齐（最大移位48位，超过则尾数为0）
4. **加法计算**：
   - 将有符号尾数转换为二进制补码形式
   - 执行有符号加法：`sum_man = prod_val + c_val`
   - 计算绝对值：`sum_man_abs = abs(sum_man)`
   - 检测溢出：`sum_man_overflow`（当和溢出到额外位时）
5. **前导零计数（LZC）**：
   - 对72位绝对值进行前导零计数（0~72）
   - 根据LZC值判断结果类型：
     - `lzc_normal`（LZC < 48）：正规数
     - `lzc_large_subnormal`（48 ≤ LZC < 72）：次正规数
     - `lzc_huge_zero`（LZC ≥ 72）：零
6. **归一化**：
   - 正规化：尾数左移LZC位，指数减去LZC
   - 次正规化：尾数左移LZC位，指数置为零
7. **舍入处理**：
   - 根据舍入模式（io_rnd）计算舍入位（guard_bit, round_bit, sticky_bit）
   - 执行舍入，可能引起尾数溢出和指数调整
8. **结果组装**：
   - 正规数：`{result_sign, exp_final, man_final}`
   - 次正规数：`{sub_sign, 8'h00, man_final}`
   - 零：32'h00000000
9. **特殊值处理**：
   - 输入NaN：输出quiet NaN（尾数最高位为1）
   - 无穷大×零：输出NaN
   - 无穷大±无穷大（符号相反）：输出NaN
   - 无穷大：输出带正确符号的无穷大

### 3.3 舍入模式（io_rnd编码）
| io_rnd | 舍入模式 | 描述 |
|--------|----------|------|
| 3'b000 | 最近偶数（Round to nearest, ties to even） | 默认模式，符合IEEE 754标准 |
| 3'b001 | 向零舍入（Round toward zero） | 截断，不进行舍入 |
| 3'b010 | 向下舍入（Round down, toward negative infinity） | 向负无穷方向舍入 |
| 3'b011 | 向上舍入（Round up, toward positive infinity） | 向正无穷方向舍入 |
| 3'b100 | 最近远离零（Round to nearest, ties away from zero） | 平局时远离零舍入 |
| 其他 | 同3'b000 | 默认最近偶数 |

### 3.4 状态标志（io_status编码）
| io_status | 含义 | 描述 |
|-----------|------|------|
| 8'h00 | 正常结果 | 计算结果为正规数、次正规数或零 |
| 8'h01 | 异常NaN | 输出为NaN（包括无效操作） |
| 8'h02 | 异常无穷大 | 输出为无穷大 |
| 8'h04 | 溢出（保留） | 结果指数超出表示范围（当前未使用，保留位） |
| 8'h08 | 下溢（保留） | 结果为次正规数或零（当前未使用，保留位） |
| 8'h10 | 不精确（保留） | 结果被舍入（当前未使用，保留位） |
| 其他 | 保留 | 未来扩展使用 |

### 3.5 特殊值处理规则
- **NaN传播**：任一输入为NaN，输出NaN（quiet NaN）
- **无穷大×零**：输出NaN
- **无穷大±无穷大（符号相反）**：输出NaN
- **无穷大±有限数**：输出同号无穷大
- **零结果符号**：强制为正零（+0）

## 4. 寄存器描述
本模块采用5级流水线设计，详细流水线结构见第5节。

## 5. 流水线设计
本模块采用5级流水线结构，每级功能如下：

| 流水线级 | 阶段名称 | 主要功能 | 关键操作 |
|----------|----------|----------|----------|
| Stage 0 | 输入解码 | 输入分解、特殊值检测、指数计算 | 符号位、指数、尾数提取；隐藏位添加；NaN/无穷大检测 |
| Stage 1 | 乘法与对齐 | 尾数乘法、指数差计算、尾数对齐 | Booth-4乘法；尾数扩展；指数差计算；移位对齐 |
| Stage 2 | 加法与LZC | 有符号加法、绝对值计算、前导零计数 | 二进制补码转换；72位加法；LZC计数（0-72） |
| Stage 3 | 规格化与舍入 | 尾数规格化、舍入处理、溢出检测 | 尾数左移；舍入位计算；尾数溢出处理 |
| Stage 4 | 结果组装 | 结果格式组装、特殊值输出、状态标志生成 | 符号位组合；指数调整；输出选择；状态标志设置 |

**流水线寄存器**：各级之间通过寄存器分隔（_r1, _r2, _r3, _r4后缀信号）。
**时序特性**：每级延迟需满足400MHz目标频率，无流水线停顿机制。
**复位影响**：异步复位resetn清除所有流水线寄存器和输出寄存器。

## 6. 性能指标
- **工艺节点**: TSMC 22nm
- **目标频率**: 400MHz
- **关键路径**: 预计为前导零计数（LZC）逻辑和舍入逻辑的级联
- **面积估算**: 较大，主要来源于72位前导零计数逻辑和24×24位乘法器
- **功耗预算**: 中等，由于组合逻辑复杂度较高
- **时序裕量**: 需要综合后评估，建议对关键路径进行流水线优化

## 7. 验证计划
### 7.1 功能点覆盖
1. **基本运算**：随机正规数乘累加，与软件模型对比
2. **舍入模式**：对每种舍入模式进行测试，包括边界情况
3. **特殊值**：
   - NaN输入与传播
   - 无穷大运算（±∞ × ±∞, ∞ × 0, ∞ ± ∞等）
   - 零运算（0 × 0, 0 ± 0等）
   - 次正规数运算
4. **边界条件**：
   - 最大/最小正规数
   - 溢出/下溢情况
   - 舍入引起的指数溢出
5. **组合场景**：随机混合正常值和特殊值

### 7.2 断言检查
1. **输入输出关系**：确保结果符合IEEE 754标准
2. **NaN传播**：任一输入NaN，输出必须为NaN
3. **无穷大一致性**：无穷大运算结果符号正确
4. **舍入模式**：根据io_rnd正确舍入
5. **状态标志**：io_status与输出类型一致

### 7.3 测试场景
1. **随机测试**：生成大量随机测试向量，与参考模型（如Python的float）比较
2. **定向测试**：
   - 乘积溢出测试
   - 加法抵消测试（如A*B + C = 0）
   - 渐进下溢测试
   - 舍入平局（ties）测试
3. **性能测试**：评估关键路径延迟，验证时序约束
4. **覆盖率目标**：
   - 代码覆盖率 > 95%
   - 功能覆盖率 > 90%
   - 断言覆盖率 100%

## 7. 注意事项
1. **组合逻辑延迟**：本模块为纯组合逻辑，关键路径较长，建议在顶层添加流水线寄存器以满足时序要求。
2. **面积优化**：前导零计数逻辑占用较大面积，可考虑使用并行前缀树结构优化。
3. **舍入模式兼容性**：io_rnd=3'b100（最近远离零）与IEEE 754标准不完全一致，平局时向零舍入而非远离零舍入，使用时需注意。
4. **零的符号**：输出零强制为正零（+0），与标准允许的负零不一致，可能影响某些应用。
5. **状态标志简化**：io_status仅指示NaN和无穷大，未包含溢出、下溢等异常标志，需要时可在上层扩展。
6. **验证完备性**：必须对所有舍入模式和特殊值组合进行充分验证，建议使用形式验证辅助。